
# 既存の _config.yml にマージして使ってください
# サイトマップに含めたくないディレクトリやファイルの一括設定例
defaults:
  - scope:
      path: "assets"
    values:
      sitemap: false
  - scope:
      path: "node_modules"
    values:
      sitemap: false
